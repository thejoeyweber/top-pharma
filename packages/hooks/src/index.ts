/**
 * @description
 * This is the main entry point for the shared hooks package (@workspace/hooks).
 * It exports custom React hooks used across different applications within the monorepo.
 *
 * @exports useIsMobile - A hook to detect if the current viewport matches mobile dimensions.
 */

export * from "./use-mobile.js"; 